# BOOLEAN

BOOLEAN stores logical values.

Possible values:

TRUE
FALSE

Example:

is_active BOOLEAN

Common uses:

- is_active
- is_verified
- has_paid
- is_available

BOOLEAN is useful for yes/no or true/false conditions.